<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.dede</groupId>
    <artifactId>dede-maven-plugin</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>maven-plugin</packaging>
    <name>dede-maven-plugin</name>
    <description>Runs Dede's AEM Cloud Service readiness and security analysis as a Maven build step, failing the build on CRITICAL findings.</description>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.plugin-api.version>3.9.6</maven.plugin-api.version>
        <maven.plugin-annotations.version>3.13.1</maven.plugin-annotations.version>
        <maven.plugin-plugin.version>3.13.1</maven.plugin-plugin.version>
    </properties>

    <dependencies>
        <!-- The Dede engine itself, as a plain library dependency. Requires
             `mvn install` on the root dede-java module first (packaging=bundle
             produces a normal, non-shaded JAR usable as a dependency; see
             CONTRIBUTING.md). Maven plugins run in an isolated Plexus/Sisu
             classloader, so pulling in dede-java's full Spring Boot dependency
             tree here does not leak into consumers' own project classpaths. -->
        <dependency>
            <groupId>com.dede</groupId>
            <artifactId>dede-java</artifactId>
            <version>0.0.1-SNAPSHOT</version>
            <exclusions>
                <!-- Maven plugins run inside Maven's own SLF4J binding
                     (maven-slf4j-provider). Bringing in Logback via Spring
                     Boot's default logging starter competes with it and
                     Spring's logging bootstrap hard-fails at startup. -->
                <exclusion>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-plugin-api</artifactId>
            <version>${maven.plugin-api.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven.plugin-tools</groupId>
            <artifactId>maven-plugin-annotations</artifactId>
            <version>${maven.plugin-annotations.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.maven</groupId>
            <artifactId>maven-core</artifactId>
            <version>${maven.plugin-api.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.1</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.24.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-plugin-plugin</artifactId>
                <version>${maven.plugin-plugin.version}</version>
                <executions>
                    <execution>
                        <id>default-descriptor</id>
                        <goals>
                            <goal>descriptor</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
            </plugin>
        </plugins>
    </build>
</project>
