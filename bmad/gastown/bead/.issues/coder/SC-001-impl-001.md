---
id: SC-001-impl-001
workflow_id: SC-001
type: implementation
agent: coder
status: pending
priority: high
created: 2026-04-08T00:00:00Z
updated: 2026-04-08T00:00:00Z
depends_on: [SC-001-spec-001]
blocks: [SC-001-test-001, SC-001-review-001]
---

# Implement: Add SUPPLY_CHAIN categories to forbidden-apis.json

## Context

Add three new forbidden-API categories to `src/main/resources/forbidden-apis.json` as specified in
`SC-001-spec-001`. No Java code changes required — `ForbiddenApiCatalog` loads the JSON at startup
via `@PostConstruct` and the new categories are automatically available.

**Spec**: bmad/gastown/bead/.issues/docs/SC-001-spec-001.md

## Acceptance Criteria

- [ ] `SUPPLY_CHAIN_PROCESS_SPAWN` category added (severity: CRITICAL, classes: Runtime + ProcessBuilder, methods: exec + start)
- [ ] `SUPPLY_CHAIN_REMOTE_CLASSLOAD` category added (severity: CRITICAL, class: URLClassLoader, method: loadClass)
- [ ] `SUPPLY_CHAIN_REFLECTION_LOAD` category added (severity: HIGH, method: forName on Class)
- [ ] JSON is valid (no parse errors on startup)
- [ ] All existing 116 tests pass: `mvn test`

## Technical Details

### File to Edit
`src/main/resources/forbidden-apis.json` — append to the `categories` array.

### JSON to Append

```json
{
  "name": "SUPPLY_CHAIN_PROCESS_SPAWN",
  "description": "OS process spawning inside an OSGi bundle is a supply chain attack indicator. AEM bundles have no legitimate reason to fork processes.",
  "severity": "CRITICAL",
  "replacement": "Use Sling Jobs (org.apache.sling.event.jobs.JobManager) for async/background work.",
  "packages": [],
  "classes": ["java.lang.Runtime", "java.lang.ProcessBuilder"],
  "methods": ["exec", "start"],
  "annotations": []
},
{
  "name": "SUPPLY_CHAIN_REMOTE_CLASSLOAD",
  "description": "Loading classes from remote URLs at runtime bypasses OSGi's class-visibility contract and is a classic supply chain backdoor delivery mechanism.",
  "severity": "CRITICAL",
  "replacement": "Declare all required packages in OSGi Import-Package. Use @Reference for service lookup.",
  "packages": [],
  "classes": ["java.net.URLClassLoader"],
  "methods": ["loadClass"],
  "annotations": []
},
{
  "name": "SUPPLY_CHAIN_REFLECTION_LOAD",
  "description": "Class.forName() with a dynamic or encoded class name is used in supply chain attacks to load payloads without static imports. Prefer explicit @Reference injection.",
  "severity": "HIGH",
  "replacement": "Use explicit class references or @Reference/@Inject for OSGi/Sling service lookup.",
  "packages": [],
  "classes": [],
  "methods": ["forName"],
  "annotations": []
}
```

### Verify After Edit
```bash
mvn test -q
# Expect: BUILD SUCCESS, 116+ tests passing
```

## Progress Log

### 2026-04-08T00:00:00Z
Issue created by Mayor.

## Handoff Notes

For tester: `ForbiddenApiCatalog` loads `forbidden-apis.json` via `@PostConstruct init()`.
Test via `ForbiddenApiCatalog.checkClass(String)` and `checkMethod(String)` — no mocking needed,
just instantiate with `new ForbiddenApiCatalog()` and call `init()`.

## Files Changed

- src/main/resources/forbidden-apis.json
