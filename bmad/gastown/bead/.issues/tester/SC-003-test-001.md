---
id: SC-003-test-001
workflow_id: SC-003
type: testing
agent: tester
status: pending
priority: high
created: 2026-04-08T00:00:00Z
updated: 2026-04-08T00:00:00Z
depends_on: [SC-003-spec-001, SC-003-impl-001]
blocks: [SC-003-review-001]
---

# Test: SupplyChainAnalyzer service

## Context

Write tests for all four risk types detected by `SupplyChainAnalyzer`.

**Implementation**: bmad/gastown/bead/.issues/coder/SC-003-impl-001.md

## Test Requirements

- [ ] Each of the 4 `RiskType` values has at least one positive test
- [ ] Negative test: clean pom with known publishers returns empty list
- [ ] `TyposquatDetector` tested in isolation (unit test the Levenshtein distance logic)
- [ ] Graph node creation verified (SUPPLY_CHAIN_RISK node present)
- [ ] Tests use `@TempDir` — no real filesystem dependencies
- [ ] All 116 baseline tests still pass

## Test Scenarios

### SNAPSHOT_IN_PRODUCTION_SCOPE
```xml
<!-- pom.xml fragment to write to @TempDir -->
<dependency>
  <groupId>com.example</groupId>
  <artifactId>some-lib</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <scope>compile</scope>
</dependency>
```
Expected: 1 risk, type=SNAPSHOT_IN_PRODUCTION_SCOPE, severity=HIGH

### TYPOSQUATTED_GROUP_ID
```xml
<dependency>
  <groupId>org.apche.sling</groupId>  <!-- typo: apche vs apache -->
  <artifactId>org.apache.sling.api</artifactId>
  <version>2.27.0</version>
</dependency>
```
Expected: 1 risk, type=TYPOSQUATTED_GROUP_ID, severity=CRITICAL

### UNVERIFIED_REPOSITORY
```xml
<repositories>
  <repository>
    <id>evil-repo</id>
    <url>http://packages.evil.example.com/maven2</url>
  </repository>
</repositories>
```
Expected: 1 risk, type=UNVERIFIED_REPOSITORY, severity=HIGH

### EMBEDDED_JAR
Write a dummy `.jar` file to `@TempDir/ui.apps/src/main/content/jcr_root/apps/myapp/install/payload.jar`
Expected: 1 risk, type=EMBEDDED_JAR, severity=HIGH

### Clean Project
Standard pom with only `org.apache.sling` and `com.adobe.aem` deps, no custom repos.
Expected: empty list

### TyposquatDetector Unit Test
| Candidate | Nearest known | Distance | Flagged? |
|-----------|--------------|----------|---------|
| `org.apche.sling` | `org.apache.sling` | 1 | yes |
| `org.apache.sling` | self | 0 | no |
| `com.totally.unknown` | any | > 1 | no |

## Test Files

- `src/test/java/com/dede/supply/SupplyChainAnalyzerTest.java`
- `src/test/java/com/dede/supply/TyposquatDetectorTest.java`

## Progress Log

### 2026-04-08T00:00:00Z
Issue created by Mayor.
